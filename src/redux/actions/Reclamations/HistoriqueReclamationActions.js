export const isLoadingChanged = (payload) => {
    return {
        type: 'CLAIM_HISTORIQUE_IS_LOADING_CHANGED',
        payload,
        info: 'This action is used to wait while loading data into the platform'
    }
};
export const isSuccessChanged = (payload) => {
    return {
        type: 'CLAIM_HISTORIQUE_IS_SUCCESS_CHANGED',
        payload,
        info: 'This action is used to wait while loading data into the platform'
    }
};
export const showMessageChanged = (payload) => {
    return {
        type: 'CLAIM_HISTORIQUE_SHOW_MESSAGE_CHANGED',
        payload,
        info: 'This action is used to wait while loading data into the platform'
    }
};
export const messageChanged = (payload) => {
    return {
        type: 'CLAIM_HISTORIQUE_MESSAGE_CHANGED',
        payload,
        info: 'This action is used to wait while loading data into the platform'
    }
};
export const itemsChanged = (payload) => {
    return {
        type: 'CLAIM_HISTORIQUE_ITEMS_CHANGED',
        payload,
        info: 'This action is used to wait while loading data into the platform'
    }
};
export const showModalChanged = (payload) => {
    return {
        type: 'CLAIM_HISTORIQUE_SHOW_MODAL_CHANGED',
        payload,
        info: 'This action is used to wait while loading data into the platform'
    }
};
export const currentItemChanged = (payload) => {
    return {
        type: 'CLAIM_HISTORIQUE_CURRENT_ITEM_CHANGED',
        payload,
        info: 'This action is used to wait while loading data into the platform'
    }
};
export const resetAll = () => {
    return {
        type: 'CLAIM_HISTORIQUE_RESET_ALL',
        payload:null,
        info: 'This action is used to wait while loading data into the platform'
    }
};

